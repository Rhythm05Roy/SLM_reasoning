# ============================================================
# CurricSym-SLM-Lite — RunPod RTX 4090 Requirements
# ============================================================
# Install order matters for CUDA compatibility.
# Use the setup script (scripts/setup_runpod.sh) for a clean install.

# ── Core ML ──────────────────────────────────────────────────
torch>=2.3.0
torchvision
torchaudio

# ── Unsloth (must be installed BEFORE transformers in RunPod) ─
# Use the nightly wheel matching your CUDA version — see unsloth.ai
# For CUDA 12.4+ / torch 2.3+ on RTX 4090:
unsloth[cu124-torch230] @ https://github.com/unslothai/unsloth/releases/download/tag-2026.2.1/unsloth-2026.2.1-py3-none-any.whl

# ── Hugging Face ecosystem ────────────────────────────────────
transformers>=4.45.0
datasets>=2.20.0
accelerate>=0.34.0
peft>=0.13.0
trl>=0.12.0
tokenizers>=0.19.0
huggingface_hub>=0.24.0
safetensors>=0.4.0

# ── Training utilities ────────────────────────────────────────
bitsandbytes>=0.43.0         # 8-bit Adam optimizer
flash-attn>=2.6.0            # Flash Attention 2 (native on RTX 4090 / Ampere)

# ── Symbolic reasoning ────────────────────────────────────────
z3-solver>=4.13.0

# ── Experiment tracking ───────────────────────────────────────
wandb>=0.18.0

# ── Data & numerics ───────────────────────────────────────────
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.13.0

# ── Visualisation ─────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.66.0
rich>=13.7.0
psutil>=5.9.0
